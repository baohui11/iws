# 复制为 docker/.env 后按需修改。docker compose 会自动读取同目录下的 .env。
POSTGRES_USER=iws
POSTGRES_PASSWORD=iws_dev_password
POSTGRES_DB=iws
POSTGRES_PORT=5432

# 对应的应用连接串（写入项目根目录 .env 的 DATABASE_URL）：
# DATABASE_URL=postgresql://iws:iws_dev_password@localhost:5432/iws

# ---- MinIO（对象存储）----
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_API_CORS_ALLOW_ORIGIN=*
S3_PROJECT_FILES_BUCKET=project-files
S3_AVATAR_BUCKET=avatars

# ---- File processing ----
GOTENBERG_PORT=3001
FILE_WORKER_POLL_INTERVAL_SECONDS=2
FILE_WORKER_VISIBILITY_TIMEOUT_SECONDS=300
FILE_WORKER_MAX_MESSAGES=5
