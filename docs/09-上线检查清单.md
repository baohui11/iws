# 上线检查清单

## 环境变量

- [ ] `AUTH_SECRET` 已设置强随机值。
- [ ] `FILE_UPLOAD_TOKEN_SECRET` 已设置强随机值。
- [ ] `APP_BASE_URL` 为正式访问地址。
- [ ] `POSTGRES_PASSWORD` 已修改。
- [ ] `MINIO_ROOT_PASSWORD` 已修改。
- [ ] `SMTP_USER`、`SMTP_PASSWORD`、`SMTP_FROM` 已配置。
- [ ] `OA_PGREST_TOKEN` 已配置。
- [ ] `PADDLEOCR_TOKEN` 已配置。
- [ ] `DASHSCOPE_API_KEY` 已配置。
- [ ] 默认管理员账号和密码已修改。

## 数据库

- [ ] Docker 启动时迁移执行成功。
- [ ] 初始管理员创建成功。
- [ ] PostgreSQL 数据卷已规划备份。
- [ ] pgvector、pgmq、pg_jieba、pg_trgm 扩展可用。

## 存储

- [ ] MinIO 数据卷已规划备份。
- [ ] `project-files` 桶可用。
- [ ] `avatars` 桶可用。
- [ ] 浏览器能访问 `S3_PUBLIC_ENDPOINT`。
- [ ] 如使用加密网关，上传和下载链路已验证。

## 文件处理

- [ ] Gotenberg 可访问。
- [ ] file-worker 正常运行。
- [ ] 上传文件后能生成预览。
- [ ] 文档解析能写入切片。
- [ ] DashScope embedding 可调用。
- [ ] 搜索可返回全文和语义结果。
- [ ] query embedding 超时或不可用时，搜索能自动降级到关键词/元数据结果。
- [ ] `EMBEDDING_SERVICE_TOKEN` 为强随机值，file-worker 的 8010 端口未对宿主机发布。
- [ ] 使用脱敏生产规模数据执行 `EXPLAIN (ANALYZE, BUFFERS)`，确认向量查询使用 HNSW 索引。

## OA 同步

- [ ] 手动同步部门成功。
- [ ] 手动同步用户成功。
- [ ] 手动同步项目成功。
- [ ] 手动同步项目角色成功。
- [ ] 一键同步全部成功。
- [ ] 定时同步时间符合预期。

## 邮件

- [ ] 忘记密码邮件可发送。
- [ ] 用户邀请邮件可发送。
- [ ] 邮件中的链接域名正确。
- [ ] 生产环境 TLS 校验未关闭。

## 权限

- [ ] 普通用户只能看自己的项目和文件。
- [ ] 部门管理员只能看权限范围内的数据。
- [ ] 部门管理员可查看 OA 同步记录但不能执行同步。
- [ ] 系统管理员可访问所有模块和所有数据。
- [ ] 保密文件权限符合预期。

## 运维

- [ ] `docker compose ps` 全部健康。
- [ ] `web` 日志无迁移错误。
- [ ] `file-worker` 日志无持续失败任务。
- [ ] 数据库和对象存储已有备份方案。
- [ ] 已记录生产 `.env` 的保管位置。
- [ ] 已对搜索 P95 延迟、embedding 失败率、处理队列积压/任务年龄、最终失败任务数设置告警。
- [ ] 已演练 worker 中断与 pgmq 投递失败；确认 stale pending task 的补偿投递正常。
- [ ] 已执行角色 × 部门范围 × 保密状态的检索、预览、下载权限回归。
